A Drupal module that hooks the Backup and Migrate module, forcing a log message entry at every "quick" backup. ("Advanced" backups can still be made the normal way.) 

Log messages are nodes themselves, so they can be served in a feed (like a changelog).
